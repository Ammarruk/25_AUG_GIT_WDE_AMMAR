Line 1 created by main branch 
Line 2 created by main branch
Git Commands Notes
==================

1. Check Git Version
--------------------
git --version
👉 Shows installed Git version.

2. Initialize Repository with Main Branch
-----------------------------------------
git init -b main
👉 Creates a new Git repository in the current folder.
- `-b main` means the default branch will be called `main` (instead of `master`).
- `main` is like the primary line of development where final code usually lives.

3. Check Status
---------------
git status
👉 Shows the status of files:
- Untracked → New file, not in Git yet.
- Modified → File changed after last commit.
- Staged → File added with `git add`, ready to be committed.

4. Add Files to Staging Area
----------------------------
git add filename.txt
👉 Adds a specific file to staging.

For all files:
git add .

5. Configure User (One-Time Setup)
----------------------------------
git config --global user.name "Ammarruk"
git config --global user.email "ammarruk2002@gmail.com"
👉 Sets your identity for commits (applies to all projects on your system).

(If you want only for the current repo, remove `--global`).

6. Commit Changes
-----------------
git commit -m "first commit"
👉 Saves a snapshot of staged files with a message.
- Think of it as "Save Point" in your project.

7. View Commit History
----------------------
git log
👉 Shows commit history with ID, author, date, and message.

For short one-line history:
git log --oneline

Git Workflow Summary
--------------------
1. git init -b main → Create repo
2. git status → Check files
3. git add . → Stage files
4. git commit -m "message" → Save snapshot
5. git log → Confirm




day2 

git add .   (all the files ) 
git remote add origin "https://github.com/Ammarruk/25_AUG_GIT_WDE_AMMAR.git"
git push origin main 
git pull origin main 
ls (showing the files )



Tags 
python version name whereever if u want to version it depent upon the user 
in v 1 we make push and pull in version 2 we are created branches we are going to push this to gitbhub account next merge  main in github main main branch 
in sub branch example : ammar woring in branch 
v 4 commit id 


git tag (for knowing how many tags )

git tag -a v1.0 -m "first push and pull " this is for creting tags   (-a means user id email and all details will be save )
git push --tags   (for showing the tags )
git branch (for knowing branch its point through the current branch )
git branch (branch name here )ammar_b1
git checkout ammar_b1
git ls-tree --name-only -r ammar_b1   (for showing files )
cat (  file name content.txt)

day3
git log readme.txt
mkdir docs_folder  (creating folder )
echo "welcome to sript,txt file under the folder "> docs_folder/script.txt


